services:
  nginx:
    image: nginx:1.27-alpine
    container_name: campus-service-domain-proxy
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
